/**
 * These routes will redirect users to default route
 * @type {string[]}
 */
export const authRoutes = ["/auth/login", "/auth/register"];

/**
 * These login page
 * @type {string}
 */
export const LOGIN_PAGE = "/auth/login";

/**
 * Routes with this prefix are for auth
 * @type {string}
 */
export const apiAuthPrefix = "/api/auth";

/**
 * These routes public
 * @type {string[]}
 */
export const publicRoutes = ["/settings"];

/**
 * Default route
 * @type {string}
 */
export const DEFAULT_ROUTE = "/";
